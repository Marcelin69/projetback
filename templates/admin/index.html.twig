{% extends '@EasyAdmin/page/content.html.twig' %}
{% block stylesheet %}
	<link rel="stylesheet" href={{ asset('css/custom.css') }}>
{% endblock %}

{% block page_content %}

	{{ dump(reservation[0].datereservation) }}
	{% set date =(reservation[0].datereservation)  %}
	{# {% set dateformat = date->format('d/m/y')  %} #}

	{# {{dateformat }} #}

	<div class="container text-center">
		<div class="row">
			<div class="col">
				<div>
					<h3>
						Nombre de salle

					</h3>
				</div>
				<div>{{ salles}}</div>
			</div>
			<div class="col">
				<div>
					<h3>
						Nombre de reservation
					</h3>
				</div>
				<div>{{reservations}}</div>
			</div>
			<div class="col">

				<div>
					<h3>
						Reservation accepter
					</h3>
				</div>
				<div>{{ accepter}}</div>
			</div>
		</div>
	</div>

	<div class="content-body contenair ms-4">

		{% for item in utilisateurs %}

			<div class="card" style="width: 18rem;">
				<div class="card-body">
					<h5 class="card-title">{{item.nom}}
						{{item.prenom}}</h5>
					<p> <span class="span">Mail:</span>
						{{item.email}}</p>
					{# <h5 class="card-title">{{item.}}</h5> #}
					<p class="card-text">
						{% for reserv in item.reservations %}

							<p>
								<span class="span">les salle reserver:</span>
								{{reserv }}
							</p>
						{% endfor %}

					</p>
					<p class="card-text">
						{% for reserv in item.reservations %}
							<p>
								<span class="span">Date reserver:</span>
								{# {{reserv.datereservation}} #}
							</p>
						{% endfor %}

					</p>
					<a href="#" class="btn btn-primary">Go somewhere</a>
				</div>
			</div>
		{% endfor %}

	</div>

{% endblock %}
